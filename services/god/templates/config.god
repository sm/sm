# This is the god configuration file for bdsm.
# It is used as a starting point for god to load all service god configs.
# To load new services simply add a line to the config file like so:
# God.load("$config_path/$service.god")
