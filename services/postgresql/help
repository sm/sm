#!/usr/bin/env bash

echo -e "
Usage
  $0 {install,setup,start,stop,help}
Actions
  install  - Install the postgresql gem.
  setup    - Place the config.postgresql template in $current_path/config/config.postgresql
  start    - Startup postgresql.
  stop     - Stop postgresql.
  help     - Display this help text.
"
exit $?
