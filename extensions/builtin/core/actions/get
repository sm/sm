#!/usr/bin/env bash

action="${extension_args[1]}"

case "${action}" in

  version) # x.y.z
    install_release
    ;;

  latest)
    fetch_version
    install_release
    ;;

  *)
    install_remote "${action:-wayneeseguin}"
    ;;
esac

