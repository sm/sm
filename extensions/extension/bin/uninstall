#!/usr/bin/env bash

[[ -n "$extensions" ]] || fail "At least one extension name must be specified."

for extension in ${extensions//,/ } ; do
  [[ -d "$extensions_path/$extension/" ]] && rm -rf "$extensions_path/$extension/"
done

succeed "Removed extensions ${extensions}"
