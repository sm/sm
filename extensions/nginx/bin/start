#!/usr/bin/env bash

source "$extension_scripts_path/initialize"

if [[ "$master_pid" -gt 0 ]] ; then
  echo -e "Nginx is already running with master pid: $master_pid"
else
  nginx -t -c $config_file && nginx -c $config_file
fi
