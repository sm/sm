#!/usr/bin/env bash

_path="${core_development_path:="$source_path/bdsm"}"

# TODO: Allow --src flag to modify root behavior.
running_as_root && _path="${bdsm_path}/"

enter "${_path}"

exec ${EDITOR:-vi} .

exit 0 # For sanity.
